spring.application.name=helpdesk-chatbot-backend
server.port=8080
# The context-path is removed because all @RequestMapping annotations already include /api.
# Keeping it would result in duplicate paths like /api/api/auth/login.

spring.datasource.url=jdbc:mysql://localhost:3306/helpdesk_chatbot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sujalkamble7
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# These properties now match the @Value annotations in JwtService.java
# The secret key should be a long, random, Base64-encoded string.
application.security.jwt.secret-key=c3VqYWxrYW1ibGVzdHZpbmNlbnRwYWxsb3R0aWNvbGxlZ2U=
application.security.jwt.expiration=3600000